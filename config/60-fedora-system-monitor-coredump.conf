# Fedora System Monitor crash evidence retention.
# Core bytes remain managed by systemd-coredump; the monitor stores only
# bounded metadata and a forensic JSON snapshot.
[Coredump]
Storage=external
Compress=yes
ProcessSizeMax=2G
ExternalSizeMax=2G
MaxUse=3G
KeepFree=5G
