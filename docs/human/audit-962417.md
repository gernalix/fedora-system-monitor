# Audit Fedora 44 — attività 962417

Esito: implementazione completata e verificata; restano soltanto Secure Boot e
il test live del Samsung T7, entrambi dipendenti dall’utente.

| Punto | Stato iniziale | Già coperto da | Problema reale | Azione |
|---|---|---|---|---|
| ZRAM | DA_CORREGGERE | zram Fedora corretta | falso alert su percentuale | pressione composta; zram invariata |
| Snapshot Btrfs | DA_IMPLEMENTARE | Btrfs/GRUB compatibili | snapshot assenti | Snapper root, pre-DNF5, retention e cleanup |
| SSD trim | DA_CORREGGERE | `fstrim.timer` | `discard=async` live | `nodiscard`; fstrim settimanale mantenuto |
| Pulizia | DA_CORREGGERE | DNF5/tmpfiles/journald | repo Terra rotto e inutilizzato | repo disabilitato; nessun dato utente eliminato |
| SMART | DA_IMPLEMENTARE | smartd e collector base | dettagli/standby mancanti | esteso il monitor senza svegliare HDD |
| Energia | GIÀ_OTTIMALE | amd_pstate_epp e TuneD | nessuno dimostrato | nessuna modifica |
| systemd | DA_CORREGGERE | timer nativi | raid-check senza array MD | timer disabilitato |
| Boot | GIÀ_OTTIMALE | grafo systemd | nessun collo sicuro | nessuna modifica |
| Btrfs | DA_IMPLEMENTARE | btrfs-progs | scrub/alert assenti | scrub mensile e alert integrati |
| Firmware | DA_IMPLEMENTARE | fwupd/LVFS | refresh disabilitato | timer refresh abilitato, no auto-update |
| Sicurezza | RICHIEDE_UTENTE | SELinux/firewall | Secure Boot disabilitato | decisione firmware e reboot richiesti |
| Journal | GIÀ_OTTIMALE | limite effettivo e spazio | nessuno | nessuna modifica |
| Batteria | DA_IMPLEMENTARE | collector esistente | salute dettagliata assente | collector e alert estesi |
| GNOME | GIÀ_OTTIMALE | Tracker/estensioni | nessuno dimostrato | estensioni richieste preservate |

## Evidenze

- 100 test automatici e 18 self-check passati.
- Collector installati `minute` e `daily`: `ok`; database integro.
- Scrub Btrfs read-only: 41,18 GiB, zero errori; device stats tutti a zero.
- Memoria: 68,752% disponibile, PSI zero, nessun OOM, pressione zero; l’alert
  Host sulla sola zram è recuperato.
- Seagate verificato con `smartctl -n standby`: è rimasto in sleep. Il Samsung T7
  era scollegato e non è stato trattato come errore.
- Boot baseline 41,343 s; nessuna modifica perché il tratto lento serve Samba
  intenzionale. Nessun riavvio è stato eseguito.

## Rollback e verifiche pendenti

I backup host sono in `/var/backups/activity-962417`. I timer aggiunti possono
essere disabilitati; `raid-check.timer` può essere riabilitato. Prima di rimuovere
Snapper vanno conservati gli snapshot desiderati.

Secure Boot è `PENDENTE_INTERAZIONE_UTENTE` e `PENDENTE_POST_REBOOT`. Il test
live del Samsung T7 è `PENDENTE_INTERAZIONE_UTENTE`. Restano reali gli alert
Storage per Seagate al 2,8% libero e per la precedente rimozione non sicura.
