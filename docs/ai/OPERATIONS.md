CADENCE.minute=CPU total,load,RAM available and used,swap,critical filesystems,essential Internet,battery,power,and monitored services
CADENCE.five_minute=All relevant local mounts and inodes,Telegram cumulative free-space delta,interface counters and rates,Wi-Fi,IP,gateway,latency,Internet,temperatures,top five processes,process and zombie counts,file descriptors,failed services,and external disks
CADENCE.fifteen_minute=Disk I/O and rates,recent kernel I/O errors,VPN,network stability,Wi-Fi disconnect count,expected mounts,and privacy-bounded copy or backup processes
CADENCE.hourly=Software/update snapshot,DNF and Flatpak history deltas,manual metadata deltas,database and journal size,non-waking SMART summary,uptime,boot,reboot advice,and kernel comparison
CADENCE.daily=Full inventories,detailed SMART/NVMe,battery health,Btrfs device and scrub health,directory sizes,DB integrity,backup,retention,daily summary,and unit validation
CADENCE.weekly=Full self-validation,systemd verify,database growth,index and integrity checks,retention,inventory comparison,configuration validation,and source availability
EVENT.device=Block and USB add,change,remove,mount,unmount,mount failure,unsafe removal,read-only transition,I/O error,USB reset,and known-label presence
EVENT.network=Wi-Fi connect/disconnect,SSID/interface/IP/default route changes,gateway and Internet transitions,VPN,NetworkManager state,suspend,and resume
EVENT.system=Boot,clean shutdown,reboot,suspend,resume,unclean boot,panic,oops,OOM,failed units,coredumps,hardware errors,critical thermal,and read-only remount
EVENT.software=DNF,RPM,PackageKit,Flatpak,manual metadata,AppImage,pip,pipx,uv,npm,Cargo,Android,GNOME extension,launcher,and icon deltas
THRESHOLD.disk=Warning below 20 percent,critical below 10,emergency below 5,absolute warning below 10 GiB,2 percent recovery hysteresis
THRESHOLD.small_fs=Filesystems at most 5 GiB use a proportional 128 MiB free floor instead of the 10 GiB absolute rule
THRESHOLD.inode=Warning below 15 percent,critical below 5,2 percent recovery hysteresis
THRESHOLD.cpu=Warning above 90 C for 300 seconds,critical above 95 C,recovery 5 C below threshold
THRESHOLD.nvme=Warning above 70 C for 300 seconds,critical above 80 C,recovery 5 C below threshold
THRESHOLD.ram=Warning above 90 percent for 300 seconds,critical above 95 percent,5 percent recovery hysteresis
THRESHOLD.memory_pressure=Zram used percent is informational and never alerts alone
THRESHOLD.memory_warning=MemAvailable below 10 percent plus PSI,swap-out,or reclaim evidence;or PSI some at least 10 percent
THRESHOLD.memory_critical=OOM delta,PSI full at least 5 percent,or MemAvailable below 5 percent with severe activity
THRESHOLD.memory_inputs=MemAvailable,PSI some/full avg10,swap in/out bytes per second,reclaim pages per second,OOM delta,zram compression and writeback
ALERT.memory_oom_risk=Existing memory.pressure_level alert is the anti-OOM guard;warning/critical transitions include MemAvailable,swap occupancy,PSI some/full,and up to three current top-RAM processes sampled only while pressure is active
THRESHOLD.smart=Critical health,spare below 10 percent,used life at least 100 percent,media errors,error log,or failed self-test alert;standby rotational devices are not woken
THRESHOLD.battery=Wear and temperature alert only when configured anomaly thresholds are crossed;missing optional temperature is informational
THRESHOLD.btrfs=Nonzero device stats or last scrub errors alert in Storage
THRESHOLD.network=Wi-Fi down 120 seconds,Internet down 180 seconds,three disconnects in 30 minutes,two recovery samples
THRESHOLD.services=Three restarts within 15 minutes indicates restart loop
ALERT.cooldown=Unchanged active alerts are not re-notified and default reminders are bounded to six hours
ALERT.recovery=Crossing hysteresis or receiving a native recovery closes the active alert and sends one recovery
ALERT.aggregate=Each Kuma category receives one health status summarizing all active category alerts
TELEGRAM.delta=Send one concise message per filesystem when absolute free-space change from the last delivered baseline is at least 1GiB;smaller changes remain cumulative
TELEGRAM.reference=First observation initializes silently;successful delivery advances baseline;failed delivery preserves baseline for retry
TELEGRAM.identity=Prefer filesystem UUID;fallback is a stable source hash;mount-point changes do not reset state;duplicate views of one filesystem collapse to one canonical mount
TELEGRAM.format=💾 mount_point: libero value; variazione signed_delta
TELEGRAM.credentials=/home/daniele/.config/codex/secrets/telegram.env canonical read-only mode 0600;never print token or chat ID
TELEGRAM.transport=telegram_notify.py compatibility API via installed top-level telegram_notify package;send_message title Fedora System Monitor
ALERT.io_recovery=Point I/O alerts recover only after a successful fifteen-minute collector sees no matching event for one complete journal lookback
ALERT.software_recovery=DNF Started is nonterminal and retried;only terminal failure opens an alert and terminal success recovers it
KUMA.system=Fedora Host monitor ID 39 at 180 second heartbeat
KUMA.storage=Fedora Storage monitor ID 40 at 480 second heartbeat
KUMA.network=Fedora Network monitor ID 41 at 180 second heartbeat
KUMA.services=Fedora Services monitor ID 42 at 180 second heartbeat
KUMA.software=Fedora Software monitor ID 43 at 5400 second heartbeat
KUMA.mapping=memory,temperature,CPU,kernel,OOM,and lifecycle map to system
KUMA.mapping=filesystem,SMART,I/O,read-only,mount,USB,and external device alerts map to storage
KUMA.mapping=Internet,gateway,Wi-Fi,VPN,and NetworkManager alerts map to network
KUMA.mapping=failed service,restart,and restart loop alerts map to services
KUMA.mapping=updates,DNF,Flatpak,inventory,and software transaction alerts map to software
SECURITY.units=NoNewPrivileges,ProtectSystem strict,ProtectHome,ProtectKernelTunables,ProtectKernelModules,ProtectControlGroups,ProtectKernelLogs,ProtectHostname,ProtectClock,RestrictSUIDSGID,RestrictNamespaces,LockPersonality,and MemoryDenyWriteExecute
SECURITY.capabilities=Base collectors receive CAP_DAC_READ_SEARCH,CAP_SETGID,and CAP_SETUID for read-only user inventory
SECURITY.smart_capabilities=Only hourly and daily receive CAP_SYS_ADMIN for native NVMe and CAP_SYS_RAWIO for USB NVMe SCSI passthrough;live sandbox tests proved each requirement
SECURITY.other_capabilities=CAP_DAC_OVERRIDE remains absent;fast,five-minute,fifteen-minute,weekly,event,device,and lifecycle units do not receive SMART-specific capabilities
SECURITY.paths=Write access is limited to state and runtime directories and sensitive home credential paths are inaccessible
SECURITY.network=Only UNIX,IPv4,IPv6,and netlink families are permitted where required
OPS.add_expected_disk=Set storage.expected_devices to an inline array such as [{ label = "Ventoy", required = true }] in /etc/fedora-system-monitor/config.toml then run config-check
OPS.add_service=Add the exact unit to services.essential or services.secondary then run config-check and collect minute
OPS.change_threshold=Edit the relevant thresholds table then run config-check and the matching collector
OPS.kuma_secret=Never copy endpoint values into repository files, issue reports, or command lines
OPS.telegram_secret=Do not regenerate,copy,source in a shell,or print the existing Telegram env file;validate only presence,mode,and key names
OPS.telegram_test=Real tests must be explicitly labeled TEST;normal regression tests mock telegram_notify.send_message
OPS.logs=Use journalctl units because no separate unbounded text log is maintained
OPS.wakeups=Approximately 60 fast timer wake-ups per hour plus one hourly run and event-native wakes; five and fifteen minute work is folded into fast wakes
OPS.udev_remove=RUN systemctl --no-block is retained only for remove because live udev and device-mapper tests prove it exits immediately and starts a bounded oneshot outside the worker
OPS.database_compact=WAL checkpoint TRUNCATE plus PRAGMA optimize reuses free pages and intentionally avoids automatic full VACUUM
OPS.dashboard=fedora-system-monitor dashboard;timeline;trends;service-history are bounded read-only SQLite views
OPS.prometheus=Start fedora-system-monitor-prometheus.service explicitly or run prometheus CLI;default bind=127.0.0.1:9109;unit remains disabled unless intentionally enabled
