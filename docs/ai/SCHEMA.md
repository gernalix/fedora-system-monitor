META.schema_version=2
META.engine=SQLite
META.path=/var/lib/fedora-system-monitor/monitor.sqlite3
DB.pragma=journal_mode WAL
DB.pragma=foreign_keys ON
DB.pragma=busy_timeout 10000ms
DB.timestamps=timestamp_utc and timestamp_local Europe/Copenhagen
TABLE.periodic_metrics=Numeric samples with category,name,value,unit,severity,source,device_id,JSON,outcome,error, cadence, and collector run
TABLE.events=Deduplicated event rows with occurrence count, first seen, last seen, stable key, device identity, JSON, outcome, and error
TABLE.alerts=Alert lifecycle with unique active key, severity, message, notification result, occurrence count, and recovery timestamp
TABLE.hardware_inventory=Versioned hardware snapshots keyed by stable device item
TABLE.software_inventory=Versioned RPM,Flatpak,AppImage,pip,pipx,uv,npm,Cargo,Android,GNOME,launcher,icon,and manual software snapshots
TABLE.collector_runs=Start,finish,duration,outcome,error,and inserted metric/event counts per scope
TABLE.schema_versions=Applied schema migrations
TABLE.config_versions=Redacted configuration hashes and snapshots
TABLE.dedup_state=Collector deltas, alert state, cooldown, journal cursors, and event coalescing state
TABLE.metric_aggregates=Daily min,max,average,count,and problematic sample count generated before retention deletion
TABLE.summaries=Daily and weekly compact JSON summaries
INDEX.metrics=timestamp,name plus timestamp,and cadence plus timestamp
INDEX.events=timestamp,name plus timestamp,and device plus timestamp
INDEX.alerts=status plus severity,key plus timestamp,and unique active alert key
INDEX.inventory=snapshot and item plus timestamp for both hardware and software
INDEX.runs=name plus timestamp
INDEX.aggregates=name plus bucket
INDEX.summaries=period plus timestamp
RETENTION.software_events=permanent
RETENTION.important_events=365 days minimum
RETENTION.alerts=730 days
RETENTION.bucket=Only complete UTC calendar days older than each cadence cutoff are processed
RETENTION.batch=Each cadence and UTC day commits independently to bound memory and writer lock duration
AGGREGATE.identity=UTC day,cadence,hostname,category,name,unit,source,and device
AGGREGATE.duplicates=Rows with identical aggregate identity and timestamp keep the latest row id and record discarded count
AGGREGATE.nulls=Sample count includes null observations while numeric sample count controls mean weighting
AGGREGATE.counters=Cumulative interface and disk counters record positive deltas and reset count without treating a reset as negative throughput
AGGREGATE.merge=Late segments merge exact min,max,mean,count,and problem seconds; p95 becomes null and percentile_95_exact=false because raw distribution is no longer complete
COMPACTION.policy=No automatic full VACUUM because it blocks event writers; freed pages stay on the freelist and are reused
BACKUP.method=SQLite online backup API followed by integrity_check
BACKUP.path=/var/lib/fedora-system-monitor/backups
EXPORT.formats=JSON,CSV,text
EXPORT.bound=default and maximum row limits prevent accidental bulk personal export
