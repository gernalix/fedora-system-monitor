META.project=Fedora System Monitor
META.slug=fedora-system-monitor
META.version=1.0.1
META.activity_id=593184
META.protocol=/home/daniele/MegaVault/ai/MEGAVAULT_PROTOCOL.md:v15
META.root=/home/daniele/MegaVault/projects/fedora-system-monitor
META.branch=codex/593184-fedora-system-monitor
META.remote=https://github.com/gernalix/fedora-system-monitor.git
META.platform=Fedora Linux 44 Workstation x86_64
META.timezone=Europe/Copenhagen
PURPOSE.primary=Permanent low-resource local host monitoring independent of graphical sessions
PURPOSE.secondary=Queryable history, anomaly alerts, hardware and software event reconstruction, and Uptime Kuma category health
STACK.runtime=Python 3.14 standard library
STACK.storage=SQLite schema v2 with WAL and foreign keys
STACK.scheduler=systemd services, timers, path units, and lifecycle unit
STACK.events=journal follow, udev rules, NetworkManager dispatcher, and system-sleep hook
STACK.remote=Uptime Kuma 2.3.2 Push Monitor protocol over the existing HTTP endpoint
STACK.dependencies=smartmontools,nvme-cli,lm_sensors,sqlite,python3-socketio provisioning-only stack
MAP.entry=src/fedora_system_monitor/app.py
MAP.capsules=src/fedora_system_monitor/capsules
MAP.config=config/fedora-system-monitor.toml
MAP.units=systemd
MAP.hooks=hooks
MAP.udev=udev/90-fedora-system-monitor.rules
MAP.install=scripts/install.sh
MAP.uninstall=scripts/uninstall.sh
MAP.tests=tests
MAP.ai_docs=docs/ai
MAP.human_docs=docs/human
ARCH.coordinator=CLI and systemd coordinator runs each collector scope in an isolated child process with a process-group timeout
ARCH.collectors=Collector failures remain local and return partial results without aborting other collectors
ARCH.events=One daemon follows selected journal sources while udev and native hooks handle immediate device, network, lifecycle, suspend, and resume events
ARCH.database=All writes use transactions and every record contains UTC and Europe/Copenhagen timestamps
ARCH.alerting=Alert signals use persistent state, duration gates, hysteresis, cooldown, deduplication, recovery, and endpoint aggregation
ARCH.notifications=Five category endpoints receive transitions immediately and bounded health heartbeats at collector cadence
ARCH.privacy=No traffic content, browser data, process arguments, remote MAC addresses, credentials, or personal file contents are stored
FLOW.minute=fast timer runs minute collection and conditionally overdue five-minute and fifteen-minute scopes in one wake-up
FLOW.hourly=hourly timer collects software deltas, update state, uptime, kernel state, SMART summary, and storage size
FLOW.daily=daily timer collects full inventories, detailed health, directory sizes, DB checks, backup, retention, and summary
FLOW.weekly=weekly timer validates units, indexes, integrity, retention growth, collector configuration, and log source availability
FLOW.event=journal, udev, NetworkManager, path, lifecycle, and sleep signals are normalized into events and alert transitions
INV.services=fedora-system-monitor-events.service,fedora-system-monitor-lifecycle.service,fedora-system-monitor-collect@.service,three device template services
INV.timers=fedora-system-monitor-fast.timer,fedora-system-monitor-hourly.timer,fedora-system-monitor-daily.timer,fedora-system-monitor-weekly.timer
INV.path=fedora-system-monitor-software.path
INV.runtime=/usr/local/libexec/fedora-system-monitor
INV.cli=/usr/local/bin/fedora-system-monitor
INV.config=/etc/fedora-system-monitor/config.toml
INV.secrets=/etc/fedora-system-monitor/uptime-kuma.toml root:root 0600
INV.state=/var/lib/fedora-system-monitor
INV.database=/var/lib/fedora-system-monitor/monitor.sqlite3 root:daniele 0640
BUILD.install=sudo ./scripts/install.sh
BUILD.update=git pull followed by sudo ./scripts/install.sh
BUILD.atomic=Runtime is staged root-owned, checked for symlinks and writable modes, then atomically replaced
BUILD.backup=Installer creates an online SQLite backup before replacement and retains ten install backups
BUILD.uninstall=sudo ./scripts/uninstall.sh preserves configuration and data
BUILD.purge=sudo ./scripts/uninstall.sh --purge-data removes configuration and monitoring data and is destructive
TEST.unit=PYTHONPATH=src PYTHONWARNINGS=error python3 -m unittest discover -s tests -q
TEST.compile=python3 -m compileall -q src
TEST.units=systemd-analyze verify /etc/systemd/system/fedora-system-monitor-*
TEST.udev=udevadm verify /etc/udev/rules.d/90-fedora-system-monitor.rules
TEST.live=sudo fedora-system-monitor test --system
TEST.database=sudo fedora-system-monitor db-check
TEST.result_2026_07_10=84 automated tests passed and all hardened live collector scopes completed with outcome ok
TEST.kuma_2026_07_10=All five UP heartbeats delivered and controlled Software DOWN then UP returned HTTP 200
DATA.schema_version=2
DATA.tables=periodic_metrics,events,alerts,hardware_inventory,software_inventory,collector_runs,schema_versions,config_versions,dedup_state,metric_aggregates,summaries
DATA.journal_mode=WAL
DATA.retention=Minute 14d, five-minute 60d, fifteen-minute 180d, hourly 365d, daily 1825d, hardware/general events 365d, alerts 730d, software events permanent
DATA.inventory_retention=Daily snapshots 35d then weekly/monthly thinning
DATA.backup_retention=14 daily,8 weekly,12 monthly
DATA.aggregation=Only complete UTC days are aggregated with deduplicated min,max,average,count,problem seconds,missing count,counter reset metadata,and exact initial p95 before deletion
DNB.secret=Never commit or print `/etc/fedora-system-monitor/uptime-kuma.toml`
DNB.devices=Never use `/dev/sdX` as durable identity
DNB.smart=Never poll SMART at minute cadence or wake sleeping rotational media unnecessarily
DNB.privacy=Never collect command lines, visited URLs, traffic content, or personal file content
DNB.git=Never commit generated database, backups, logs, or endpoint files
DNB.remove=Never use `--purge-data` without explicit destructive intent
BUG.resolved_backfill=Journal replay now uses immutable cursor identities and consolidates legacy duplicates transactionally
BUG.resolved_readonly=Unprivileged reporting uses immutable short-lived SQLite snapshots to avoid WAL shared-memory writes
BUG.resolved_services=Systemd template units are excluded from automatic service discovery
BUG.resolved_udev_instance=Device units use literal percent-i instances so USB and device-mapper names retain hyphens and stable metadata
BUG.resolved_alert_race=Atomic alert transitions and endpoint notification reconciliation prevent a slow DOWN from arriving after recovery
BUG.resolved_retention_boundary=Retention waits for complete UTC buckets and processes backlog one day per transaction
BUG.resolved_aggregate_merge=Late aggregate merges update canonical value and numeric-weighted average while marking merged p95 non-exact
BUG.resolved_vacuum=Weekly compact checkpoints WAL and optimizes indexes without a blocking full VACUUM
RISK.http_kuma=Existing Kuma transport is HTTP and exposes push requests to networks on path despite root-only local storage
RISK.chrome_session=Chrome administrative session expired after provisioning and is not stored or required for runtime pushes
RISK.rtc=Host RTC localtime caused historical journal realtime discontinuity and cursor identity is therefore canonical for journal import
RISK.current_host=Seagate free percentage, swap use, high CPU temperature, SPD sensor alarm, and a historical disk I/O incident are active host conditions rather than collector failures
RISK.backup_capacity=Measured 365-day database projection is about 1.16GB and the configured full-backup retention has an upper bound near 39.3GB before filesystem compression
ROAD.v1=Maintain compatibility with Fedora and Kuma updates and tune thresholds from observed baselines
ROAD.future_https=Migrate Kuma to HTTPS before treating transport confidentiality as complete
ROAD.no_feature=No additional agents, dashboards, containers, or external telemetry are planned
LINK.schema=docs/ai/SCHEMA.md
LINK.operations=docs/ai/OPERATIONS.md
LINK.incidents=docs/ai/INCIDENT_REGISTRY.md
LINK.human=docs/human/overview.md
LINK.kuma=docs/human/uptime-kuma.md
LINK.troubleshooting=docs/human/troubleshooting.md
LINK.audit=docs/ai/AUDIT_593184.md
OPEN.kuma_auth=No manual action is required for runtime pushes; an authenticated Kuma session is required only to edit monitor definitions later
OPEN.kuma_https=TLS migration remains recommended because the existing instance exposes only HTTP
OPEN.hardware_incident=Root cause of the 2026-07-09 external NTFS disconnect remains unknown
